version: 2
updates:
  - package-ecosystem: "cargo"
    directory: "/"
    schedule:
      interval: "weekly"
    # Limit the number of open PRs to reduce dependency noise
    open-pull-requests-limit: 1
    # Group updates by minor/patch versions
    versioning-strategy: "increase"
    cooldown:
      default-days: 7
